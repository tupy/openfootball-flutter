name: openfootball
description: A Flutter api client for Open Football Data. A high-level client to fetch data from HTTP resources.
version: 0.1.2
repository: https://github.com/tupy/openfootball-flutter

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.2.3
  http: ^0.11.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.2
  pedantic: ^1.9.0

# The following section is specific to Flutter.
flutter:

